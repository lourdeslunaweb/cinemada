import { Login } from "../../components"
import { Layout } from "../../components/layout"

const LoginPage = () => {
    return (
        <Layout hideNavbar hideFooter>
            <Login/>
        </Layout>
    )
}

export { LoginPage }
